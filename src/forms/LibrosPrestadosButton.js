import React from 'react';

const LibrosPrestadosButton = (props) => {
  return <button className='btn btn-secondary btn-sm' onClick={props.librosPrestados}>Libros Prestados</button>
}

export default LibrosPrestadosButton;